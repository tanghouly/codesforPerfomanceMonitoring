# codesforPerfomanceMonitoring
Both matlab and arduino codes used to retrieve data from the system.
After uploading the code in Arduino use open up matlab and use the given script to read all the session files recorded to then download them and either graph all or graph one session
